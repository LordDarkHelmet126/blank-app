import { readFileSync } from "node:fs";
import { loadContent } from "../js/content.js";
import { validateWorld, worldCatalog, worldCounts, toOfficerRecords, requiredCommandSlots } from "../js/world.js";
import { atlasMode } from "../js/world-atlas.js";

function assert(cond, msg) {
  if (!cond) throw new Error(msg);
}

const officers = JSON.parse(readFileSync(new URL("../data/officers.json", import.meta.url), "utf8"));
const regions = JSON.parse(readFileSync(new URL("../data/regions.json", import.meta.url), "utf8"));
const personalities = new Set(Object.keys(officers.personalities || {}));
const officerNames = new Set(officers.officers.map((o) => o.name));
const campaignIds = new Set(regions.regions.map((r) => r.id));

const catalog = worldCatalog();
const errors = validateWorld(catalog, { personalities, officerNames, campaignIds });
if (errors.length) {
  console.error(errors.join("\n"));
  throw new Error(`world catalog failed ${errors.length} checks`);
}

const counts = worldCounts(catalog);
assert(counts.length >= 1, "at least one world region");
const us = counts.find((c) => c.id === "us");
assert(us, "United States region");
assert(us.states === 50, `50 states, got ${us.states}`);
assert(us.districts === 1, `DC counted apart from states, got ${us.districts}`);
assert(us.subdivisions === 51, `50 states + DC, got ${us.subdivisions}`);
assert(us.territories >= 100, `major-city atlas, got ${us.territories}`);
assert(us.officers >= 10, `command roster, got ${us.officers}`);
assert(us.playable === 0, "atlas cities stay unplayable until a later reach");

const content = await loadContent();
assert(content.world.regions.find((r) => r.id === "us"), "loadContent attaches the world catalog");
assert(content.officers.officers.length === officers.officers.length, "world officers stay off the week-0 roster");
const dormant = toOfficerRecords(content.world.regions[0]);
assert(dormant.every((o) => o.dormant && o.world && o.fictional && o.faction == null), "dormant officer shape");
assert(dormant.every((o) => personalities.has(o.personality)), "personalities slot into the officer system");

const ia = content.world.regions[0].territories.find((t) => t.id === "us.des_moines");
assert(ia.yields.includes("corn") && ia.yields.includes("hogs"), "Iowa is corn and hogs");
const wy = content.world.regions[0].territories.find((t) => t.id === "us.cheyenne");
assert(wy.yields.includes("coal") && wy.yields.includes("oil") && wy.yields.includes("cattle"), "Wyoming is coal, oil, and cattle");
const nome = content.world.regions[0].territories.find((t) => t.id === "us.nome");
assert(!nome.neighbors.some((n) => n.kind === "road"), "Nome has no invented highway");
const juneau = content.world.regions[0].territories.find((t) => t.id === "us.juneau");
assert(juneau.neighbors.some((n) => n.id === "us.seattle" && n.kind === "sea"), "Juneau–Seattle is a sea lane");
assert(!regions.regions.some((r) => String(r.id).startsWith("cu.") || String(r.id).startsWith("mx.")), "atlas ids stay off the campaign board");
assert(campaignIds.has("havana") && campaignIds.has("far_cuba") && campaignIds.has("managua"), "sealift desks stay on the campaign board");

const usRegion = content.world.regions.find((r) => r.id === "us");
const byId = new Map(usRegion.territories.map((t) => [t.id, t]));
assert(byId.get("us.key_west").neighbors.some((n) => n.id === "cu.havana" && n.kind === "sea"), "Florida Straits use the Gulf Sealift desk");
const attu = byId.get("us.attu");
assert(attu && attu.neighbors.every((n) => n.kind === "sea"), "Attu is sea-only");
assert(byId.get("us.adak").neighbors.every((n) => n.kind === "sea"), "Adak is sea-only");
assert(!byId.get("us.el_paso").neighbors.some((n) => n.id === "us.san_antonio"), "I-10 does not leap El Paso to San Antonio");
assert(byId.get("us.fort_stockton").neighbors.some((n) => n.id === "us.el_paso"), "Fort Stockton sits on I-10");
assert(!byId.get("us.jacksonville").neighbors.some((n) => n.id === "us.miami"), "I-95 does not leap Jacksonville to Miami");
assert(byId.get("us.viburnum").yields.includes("lead") && byId.get("us.joplin").yields.includes("lead"), "Missouri lead belt is on the map");
const areas = ["northeast", "southeast", "midwest", "plains", "mountain", "southwest", "pacific", "alaska_hawaii"];
areas.forEach((area) => {
  const n = usRegion.officers.filter((o) => o.area === area).length;
  assert(n >= 2, `${area} needs notable officers, got ${n}`);
});
assert(usRegion.officers.some((o) => o.service === "occupation"), "occupation command");
assert(usRegion.officers.filter((o) => o.service === "resistance").length >= 8, "resistance command");
assert(us.officers >= 40, `expanded US roster, got ${us.officers}`);

const ca = counts.find((c) => c.id === "ca");
assert(ca, "Canada region");
const caRegion = content.world.regions.find((r) => r.id === "ca");
assert(caRegion.subdivisions.filter((s) => s.kind === "province").length === 10, "10 provinces");
assert(caRegion.subdivisions.filter((s) => s.kind === "territory").length === 2, "Yukon and NWT; Nunavut is still inside NWT");
assert(!caRegion.subdivisions.some((s) => s.id === "NU"), "no separate Nunavut in the 1980s");
assert(ca.playable === 0, "Canada stays unplayable");
assert(ca.officers >= 10, "Canadian command roster");
const caBy = new Map(caRegion.territories.map((t) => [t.id, t]));
const calgary = caBy.get("ca.calgary");
assert(calgary.yields.includes("oil") && calgary.yields.includes("cattle") && calgary.yields.includes("wheat"), "Calgary oil, cattle, wheat");
assert(caBy.get("ca.edmonton").yields.includes("natural_gas"), "Edmonton gas");
const regina = caBy.get("ca.regina");
assert(regina.yields.includes("wheat") && regina.yields.includes("potash"), "Regina wheat and potash");
assert(caBy.get("ca.la_ronge").yields.includes("uranium"), "Saskatchewan uranium");
assert(caBy.get("ca.sudbury").yields.includes("nickel"), "Sudbury nickel");
assert(caBy.get("ca.saguenay").yields.includes("aluminum") && caBy.get("ca.saguenay").yields.includes("hydro"), "Saguenay aluminum and hydro");
assert(caBy.get("ca.thetford_mines").yields.includes("asbestos"), "Thetford asbestos");
assert(caBy.get("ca.vancouver").yields.includes("timber"), "BC timber");
assert(caBy.get("ca.sydney").yields.includes("coal"), "Cape Breton coal");
assert(caBy.get("ca.whitehorse").yields.includes("lead") && caBy.get("ca.dawson").yields.includes("gold"), "Yukon lead and gold");
const pei = caBy.get("ca.charlottetown");
assert(pei.neighbors.every((n) => n.kind === "sea"), "PEI is a ferry, not a road");
assert(caBy.get("ca.port_aux_basques").neighbors.some((n) => n.id === "ca.sydney" && n.kind === "sea"), "Newfoundland ferry");
assert(!caBy.get("ca.port_aux_basques").neighbors.some((n) => n.kind === "road" && n.id.startsWith("ca.sydney")), "no causeway to Newfoundland");
const windsor = caBy.get("ca.windsor");
assert(windsor.neighbors.some((n) => n.id === "us.detroit" && n.kind === "road"), "Detroit–Windsor bridge");
assert(byId.get("us.detroit").neighbors.some((n) => n.id === "ca.windsor" && n.kind === "road"), "bridge is symmetric");
assert(byId.get("us.seattle").neighbors.some((n) => n.id === "ca.vancouver"), "I-5 / BC-99");
assert(byId.get("us.tok").neighbors.some((n) => n.id === "ca.whitehorse" && n.kind === "road"), "Alaska Highway");
assert(caRegion.officers.some((o) => o.slot === "head_of_state" && o.region === "ca.ottawa"), "Canadian government in Ottawa");
const caDormant = toOfficerRecords(caRegion);
assert(caDormant.every((o) => o.dormant && o.world && o.fictional && o.faction == null), "Canadian officers stay dormant");

const worldBy = new Map(content.world.regions.flatMap((r) => r.territories.map((t) => [t.id, t])));
const mx = content.world.regions.find((r) => r.id === "mx");
assert(mx.subdivisions.filter((s) => s.kind === "state").length === 31, "31 Mexican states");
assert(mx.subdivisions.some((s) => s.id === "MX-DIF" && s.kind === "district"), "Federal District");
assert(!mx.subdivisions.some((s) => s.id === "NL" || s.id === "BC"), "Mexican codes do not collide with Canada");
assert(worldBy.get("mx.campeche").yields.includes("oil") && worldBy.get("mx.carmen").yields.includes("oil"), "Campeche oil");
assert(worldBy.get("mx.villahermosa").yields.includes("oil") && worldBy.get("mx.veracruz").yields.includes("oil"), "Tabasco and Veracruz oil");
assert(worldBy.get("mx.zacatecas").yields.includes("silver") && worldBy.get("mx.guanajuato").yields.includes("silver"), "Zacatecas and Guanajuato silver");
assert(worldBy.get("mx.tuxtla").yields.includes("coffee"), "Chiapas coffee");
assert(worldBy.get("us.san_diego").neighbors.some((n) => n.id === "mx.tijuana" && n.kind === "road"), "San Ysidro");
assert(worldBy.get("us.nogales").neighbors.some((n) => n.id === "mx.nogales" && n.kind === "road"), "Nogales");
assert(worldBy.get("us.el_paso").neighbors.some((n) => n.id === "mx.juarez" && n.kind === "road"), "El Paso–Juárez");
assert(worldBy.get("us.laredo").neighbors.some((n) => n.id === "mx.nuevo_laredo" && n.kind === "road"), "Laredo");
assert(worldBy.get("us.brownsville").neighbors.some((n) => n.id === "mx.matamoros" && n.kind === "road"), "Brownsville–Matamoros");
assert(worldBy.get("mx.tijuana").neighbors.some((n) => n.id === "us.san_diego"), "border links are symmetric");

const cu = content.world.regions.find((r) => r.id === "cu");
assert(cu.subdivisions.filter((s) => s.kind === "province").length === 14, "14 Cuban provinces");
assert(cu.subdivisions.some((s) => s.id === "CU-IJ"), "Isla de la Juventud");
assert(!cu.subdivisions.some((s) => /artemisa|mayabeque/i.test(s.name)), "no 2011 Cuban provinces");
assert(worldBy.get("cu.moa").yields.includes("nickel"), "Moa nickel");
assert(worldBy.get("cu.pinar").yields.includes("tobacco"), "Pinar tobacco");
assert(worldBy.get("cu.matanzas").yields.includes("citrus"), "Matanzas citrus");
assert(worldBy.get("mx.progreso").neighbors.some((n) => n.id === "cu.pinar" && n.kind === "sea"), "Yucatán Channel");
assert(worldBy.get("cu.havana").campaignId === "havana", "Havana keeps the campaign id");
assert(worldBy.get("ni.managua").campaignId === "managua", "Managua keeps the campaign id");

assert(content.world.regions.find((r) => r.id === "gt").subdivisions.length === 22, "22 Guatemalan departments");
assert(content.world.regions.find((r) => r.id === "bz").subdivisions.length === 6, "6 Belize districts");
assert(content.world.regions.find((r) => r.id === "hn").subdivisions.length === 18, "18 Honduran departments");
assert(content.world.regions.find((r) => r.id === "sv").subdivisions.length === 14, "14 Salvadoran departments");
assert(content.world.regions.find((r) => r.id === "ni").subdivisions.some((s) => s.id === "NI-ZE"), "Zelaya is still one department");
assert(!content.world.regions.find((r) => r.id === "ni").subdivisions.some((s) => /RAAN|RAAS|Caribe/i.test(s.name)), "no split autonomous regions");
assert(content.world.regions.find((r) => r.id === "cr").subdivisions.length === 7, "7 Costa Rican provinces");
assert(content.world.regions.find((r) => r.id === "pa").subdivisions.some((s) => s.id === "PA-CZ"), "Canal Area");
assert(!worldBy.get("pa.yaviza").neighbors.some((n) => n.id.includes("colombia")), "no Darién road");
assert(worldBy.get("jm.mandeville").yields.includes("bauxite"), "Jamaica bauxite");
assert(worldBy.get("tt.point_fortin").yields.includes("oil") && worldBy.get("tt.mayaro").yields.includes("natural_gas"), "Trinidad oil and gas");
assert(worldBy.get("do.bonao").yields.includes("nickel") && worldBy.get("do.cotui").yields.includes("gold"), "Dominican nickel and gold");
assert(worldBy.get("do.san_pedro").yields.includes("sugarcane"), "Dominican sugar");
assert(content.world.regions.filter((r) => ["mx", "gt", "bz", "hn", "sv", "ni", "cr", "pa", "cu", "ht", "do", "jm", "pr", "bs", "vi", "vg", "ai", "kn", "ag", "ms", "gp", "dm", "mq", "lc", "vc", "bb", "gd", "tt"].includes(r.id)).every((r) => r.playable === false), "Region 3 stays unplayable");
const mxCount = counts.find((c) => c.id === "mx");
assert(mxCount.territories >= 90, `Mexico needs about 90 cities, got ${mxCount.territories}`);
assert(mxCount.officers >= 28 && mxCount.officers <= 36, `Mexico roster scales up, got ${mxCount.officers}`);
assert(cu.officers.length >= 8, `Cuba is large enough for a wider staff, got ${cu.officers.length}`);
assert(content.world.regions.find((r) => r.id === "ai").officers.length === 2, "Anguilla keeps a two-officer post");
assert(content.world.regions.find((r) => r.id === "bz").officers.length === 3, "Belize scales down from a full staff");
content.world.regions.forEach((r) => {
  requiredCommandSlots(r.territories.length).forEach((slot) => assert(r.officers.some((o) => o.slot === slot), `${r.id} has ${slot}`));
  assert(toOfficerRecords(r).every((o) => o.dormant && o.world && o.fictional && o.faction == null), `${r.id} officers stay dormant`);
});

const br = content.world.regions.find((r) => r.id === "br");
assert(br.subdivisions.filter((s) => s.kind === "state").length === 23, "23 Brazilian states");
assert(!br.subdivisions.some((s) => /tocantins/i.test(s.name)), "Tocantins is not a state yet");
assert(br.subdivisions.filter((s) => s.kind === "territory").map((s) => s.id).sort().join() === "BR-AP,BR-FN,BR-RR", "Amapá, Roraima, Fernando de Noronha");
assert(br.subdivisions.some((s) => s.id === "BR-DF" && s.kind === "district"), "Distrito Federal");
assert(worldBy.get("br.porto_nacional").subdivision === "BR-GO", "Porto Nacional is still Goiás");
assert(worldBy.get("br.carajas").yields.includes("iron"), "Carajás iron");
assert(worldBy.get("br.ribeirao").yields.includes("coffee") && worldBy.get("br.londrina").yields.includes("soybeans"), "coffee and soy");
assert(worldBy.get("br.recife").yields.includes("sugarcane") && worldBy.get("br.sao_bernardo").yields.includes("autos"), "sugar and autos");
assert(worldBy.get("br.santarem").yields.includes("timber") && worldBy.get("br.foz").yields.includes("hydro"), "timber and Itaipú");
assert(worldBy.get("br.noronha").neighbors.every((n) => n.kind === "sea"), "Noronha is sea-only");
const ar = content.world.regions.find((r) => r.id === "ar");
assert(ar.subdivisions.filter((s) => s.kind === "province").length === 22, "22 Argentine provinces");
assert(ar.subdivisions.some((s) => s.id === "AR-TF" && s.kind === "territory"), "Tierra del Fuego is still a territory");
assert(ar.subdivisions.some((s) => s.id === "AR-CF" && s.kind === "district"), "Capital Federal");
assert(worldBy.get("ar.rosario").yields.includes("cattle") && worldBy.get("ar.rosario").yields.includes("wheat"), "Argentine beef and wheat");
assert(worldBy.get("ar.comodoro").yields.includes("oil") && worldBy.get("ar.viedma").yields.includes("wool"), "Patagonian oil and wool");
const cl = content.world.regions.find((r) => r.id === "cl");
assert(cl.subdivisions.length === 13, "13 Chilean regions");
assert(!cl.subdivisions.some((s) => /Los Ríos|Ñuble|Parinacota/i.test(s.name)), "no post-1974 Chilean regions");
assert(worldBy.get("cl.calama").yields.includes("copper") && worldBy.get("cl.iquique").yields.includes("nitrates"), "Chilean copper and nitrates");
assert(worldBy.get("cl.talca").yields.includes("grapes") && worldBy.get("cl.talca").yields.includes("apples"), "Chilean wine grapes and fruit");
assert(worldBy.get("ar.mendoza").neighbors.some((n) => n.id === "cl.los_andes" && n.kind === "road"), "Cristo Redentor");
assert(!worldBy.get("pa.yaviza").neighbors.some((n) => n.id.startsWith("co.")), "no Darién road");
assert(worldBy.get("pa.colon").neighbors.some((n) => n.id === "co.cartagena" && n.kind === "sea"), "Panama–Colombia is a sea lane");
assert(worldBy.get("ec.puerto_ayora").neighbors.every((n) => n.kind === "sea"), "Galápagos are sea-only");
assert(worldBy.get("ec.puerto_baquerizo").neighbors.every((n) => n.kind === "sea"), "Galápagos capital is sea-only");
assert(worldBy.get("fk.stanley").neighbors.every((n) => n.kind === "sea"), "Falklands are sea-only");
assert(!worldBy.get("fk.stanley").neighbors.some((n) => n.id.startsWith("ar.")), "no Falklands road to Argentina");
assert(worldBy.get("ve.maracaibo").yields.includes("oil") && worldBy.get("ve.ciudad_guayana").yields.includes("iron") && worldBy.get("ve.ciudad_guayana").yields.includes("bauxite"), "Venezuelan oil, iron, bauxite");
assert(worldBy.get("co.manizales").yields.includes("coffee") && worldBy.get("co.albania").yields.includes("coal") && worldBy.get("co.muzo").yields.includes("emeralds") && worldBy.get("co.barrancabermeja").yields.includes("oil"), "Colombian coffee, coal, emeralds, oil");
assert(worldBy.get("pe.moquegua").yields.includes("copper") && worldBy.get("pe.cerro_de_pasco").yields.includes("silver") && worldBy.get("pe.chimbote").yields.includes("fisheries"), "Peruvian copper, silver, fishmeal");
assert(worldBy.get("bo.oruro").yields.includes("tin") && worldBy.get("bo.tarija").yields.includes("natural_gas"), "Bolivian tin and gas");
assert(worldBy.get("py.encarnacion").yields.includes("cotton") && worldBy.get("py.ciudad_del_este").yields.includes("soybeans") && worldBy.get("py.ciudad_del_este").yields.includes("hydro"), "Paraguayan cotton, soy, Itaipú");
assert(worldBy.get("uy.montevideo").yields.includes("cattle") && worldBy.get("uy.montevideo").yields.includes("wool"), "Uruguayan beef and wool");
assert(worldBy.get("gy.linden").yields.includes("bauxite") && worldBy.get("sr.paranam").yields.includes("bauxite") && worldBy.get("sr.nieuw_nickerie").yields.includes("rice"), "Guianas bauxite, rice");
assert(["co", "ve", "gy", "sr", "gf", "ec", "pe", "bo", "br", "py", "uy", "ar", "cl", "fk"].every((id) => content.world.regions.find((r) => r.id === id).playable === false), "South America stays unplayable");

const fr = content.world.regions.find((r) => r.id === "fr");
assert(fr.subdivisions.length === 96, "96 French metropolitan departments");
assert(new Set(fr.subdivisions.map((s) => s.group).filter(Boolean)).size === 22, "22 French regions of 1982");
assert(fr.subdivisions.some((s) => s.id === "FR-2A" && s.group === "Corse") && fr.subdivisions.some((s) => s.id === "FR-2B"), "Corsica is two departments");
assert(worldBy.get("fr.ajaccio").neighbors.some((n) => n.id === "fr.bastia" && n.kind === "road"), "Corsica's T20 is a road");
assert(worldBy.get("fr.marseille").neighbors.some((n) => n.id === "fr.ajaccio" && n.kind === "sea"), "Marseille–Ajaccio is a ferry");
assert(worldBy.get("gb.dover").neighbors.some((n) => n.id === "fr.calais" && n.kind === "sea"), "Dover–Calais is a ferry");
assert(!worldBy.get("gb.dover").neighbors.some((n) => n.id === "fr.calais" && n.kind === "road"), "no Channel Tunnel");
const gb = content.world.regions.find((r) => r.id === "gb");
assert(["GB-ANT", "GB-ARM", "GB-DOW", "GB-FER", "GB-LDY", "GB-TYR"].every((id) => gb.subdivisions.some((s) => s.id === id)), "Northern Ireland's six counties");
assert(gb.subdivisions.some((s) => s.id === "GB-LTN"), "Scotland's Lothian region");
const de = content.world.regions.find((r) => r.id === "de");
assert(de.subdivisions.filter((s) => s.kind === "state").length === 10, "ten West German Länder");
assert(!de.subdivisions.some((s) => /berlin/i.test(s.name)), "West Berlin is not a Land");
assert(worldBy.get("de.bonn").subdivision === "DE-NW", "Bonn sits in North Rhine-Westphalia");
assert(content.world.regions.some((r) => r.id === "wb"), "West Berlin is its own enclave");
assert(worldBy.get("de.hamburg").neighbors.some((n) => n.id === "wb.west_berlin" && n.kind === "air"), "northern air corridor");
assert(worldBy.get("de.hannover").neighbors.some((n) => n.id === "wb.west_berlin" && n.kind === "air"), "central air corridor");
assert(worldBy.get("de.frankfurt").neighbors.some((n) => n.id === "wb.west_berlin" && n.kind === "air"), "southern air corridor");
assert(worldBy.get("de.helmstedt").neighbors.some((n) => n.id === "dd.marienborn" && n.kind === "road"), "Helmstedt–Marienborn transit");
assert(worldBy.get("wb.west_berlin").neighbors.some((n) => n.id === "dd.east_berlin" && n.kind === "trail"), "Checkpoint Charlie is not an open road");
assert(!worldBy.get("wb.west_berlin").neighbors.some((n) => n.id === "dd.east_berlin" && n.kind === "road"), "no open sector road");
assert(content.world.regions.find((r) => r.id === "dd").subdivisions.length === 15, "fourteen Bezirke plus East Berlin");
assert(worldBy.get("dk.vejle").neighbors.some((n) => n.id === "dk.odense" && n.kind === "road"), "Little Belt bridges");
assert(worldBy.get("dk.korsor").neighbors.some((n) => n.id === "dk.nyborg" && n.kind === "sea"), "Great Belt is still a ferry");
assert(worldBy.get("dk.helsingor").neighbors.some((n) => n.id === "se.helsingborg" && n.kind === "sea"), "Øresund is still a ferry");
assert(worldBy.get("no.longyearbyen").neighbors.every((n) => n.kind === "sea"), "Svalbard is sea-only");
assert(worldBy.get("dk.nuuk").neighbors.every((n) => n.kind === "sea"), "Greenland is sea-only");
assert(worldBy.get("dk.torshavn").neighbors.every((n) => n.kind === "sea"), "Faroes are sea-only");
assert(worldBy.get("es.las_palmas").neighbors.every((n) => n.kind === "sea"), "Canaries are sea-only");
assert(worldBy.get("es.palma").neighbors.every((n) => n.kind === "sea"), "Balearics are sea-only");
assert(worldBy.get("pt.ponta_delgada").neighbors.every((n) => n.kind === "sea"), "Azores are sea-only");
assert(worldBy.get("pt.funchal").neighbors.every((n) => n.kind === "sea"), "Madeira is sea-only");
assert(worldBy.get("gi.gibraltar").neighbors.some((n) => n.id === "es.algeciras" && n.kind === "road"), "Gibraltar land frontier");
assert(worldBy.get("es.algeciras").neighbors.some((n) => n.id === "es.ceuta" && n.kind === "sea"), "Strait of Gibraltar ferry");
assert(content.world.regions.find((r) => r.id === "es").subdivisions.some((s) => s.id === "ES-ML"), "Melilla");
assert(worldBy.get("tr.istanbul").neighbors.some((n) => n.id === "tr.uskudar" && n.kind === "road"), "Boğaziçi Bridge");
assert(worldBy.get("tr.istanbul").neighbors.some((n) => n.id === "tr.canakkale" && n.kind === "sea"), "Bosporus and Dardanelles");
const tr = content.world.regions.find((r) => r.id === "tr");
assert(tr.subdivisions.length === 67, "67 Turkish provinces");
assert(!tr.subdivisions.some((s) => /Batman|Aksaray|Bayburt|Karaman|Kırıkkale|Şırnak/.test(s.name)), "no late-1989 Turkish provinces");
assert(![...worldBy.keys()].filter((id) => id.startsWith("cy.")).some((id) => worldBy.get(id).neighbors.some((n) => n.id.startsWith("nc."))), "no Green Line crossing");
assert(worldBy.get("it.messina").neighbors.some((n) => n.id === "it.reggio" && n.kind === "sea"), "Strait of Messina is a ferry");
assert(worldBy.get("fr.chamonix").neighbors.some((n) => n.id === "it.courmayeur" && n.kind === "road"), "Mont Blanc tunnel");
assert(worldBy.get("at.innsbruck").neighbors.some((n) => n.id === "it.bolzano" && n.kind === "road"), "Brenner");
assert(worldBy.get("fr.hendaye").neighbors.some((n) => n.id === "es.irun" && n.kind === "road"), "Hendaye–Irun");
assert(["gb", "ie", "im", "je", "gg", "gi", "fr", "mc", "ad", "nl", "be", "lu", "de", "dd", "wb", "ch", "at", "li", "it", "sm", "va", "es", "pt", "dk", "no", "se", "fi", "is", "mt", "cy", "nc", "gr", "tr"].every((id) => content.world.regions.find((r) => r.id === id).playable === false), "Western Europe stays unplayable");

function rosterSize(n) {
  if (n <= 2) return 2;
  if (n <= 7) return 3;
  if (n >= 80) return 12;
  if (n >= 40) return 8;
  if (n >= 25) return 7;
  if (n >= 15) return 6;
  return 5;
}

const eastIds = ["pl", "cs", "hu", "ro", "bg", "yu", "al", "ru", "ua", "by", "md", "ee", "lv", "lt", "ge", "am", "az", "kz", "uz", "kg", "tj", "tm"];
const eastExpect = {
  pl: [53, 8], cs: [13, 5], hu: [21, 6], ro: [41, 8], bg: [15, 6], yu: [42, 8], al: [26, 7],
  ru: [110, 12], ua: [29, 7], by: [6, 3], md: [12, 5], ee: [16, 6], lv: [26, 7], lt: [44, 8],
  ge: [6, 3], am: [3, 3], az: [5, 3], kz: [19, 6], uz: [13, 5], kg: [5, 3], tj: [5, 3], tm: [6, 3],
};
const earlySubs = new Set(["us", "ca"].flatMap((id) => content.world.regions.find((r) => r.id === id).subdivisions.map((s) => s.id)));
eastIds.forEach((id) => {
  const region = content.world.regions.find((r) => r.id === id);
  const count = counts.find((c) => c.id === id);
  const [cities, officers] = eastExpect[id];
  assert(region && region.playable === false && region.status === "occupied" && region.reach === "later", `${id} stays dormant`);
  assert(count.territories === cities && count.officers === officers, `${id} is ${cities} cities and ${officers} officers, got ${count.territories}/${count.officers}`);
  assert(count.officers === rosterSize(count.territories) && count.playable === 0, `${id} roster stays normalized`);
  assert(region.subdivisions.every((s) => !earlySubs.has(s.id) && s.id.startsWith(`${region.country}-`)), `${id} subdivision ids stay prefixed`);
  const records = toOfficerRecords(region);
  assert(records.length === officers && records.every((o) => o.dormant && o.world && o.fictional && o.faction == null), `${id} officers stay off the week-0 list`);
  assert(region.officers.every((o) => /fiction/i.test(o.bio)), `${id} bios stay fictional`);
});
assert(!content.world.regions.some((r) => r.id === "cz" || r.id === "sk"), "Czechoslovakia is still one country");
assert(!["cn", "jp", "mn", "pk"].some((id) => content.world.regions.some((r) => r.id === id)), "China, Japan, Mongolia, and Pakistan stay off this sheet");

const pl = content.world.regions.find((r) => r.id === "pl");
assert(pl.subdivisions.length === 49 && pl.subdivisions.every((s) => s.kind === "voivodeship"), "49 Polish voivodeships");
assert(!pl.territories.some((t) => t.neighbors.some((n) => n.id.startsWith("de.") || n.id.startsWith("wb."))), "Poland meets the GDR, not West Germany or West Berlin");
assert(worldBy.get("dd.gorlitz").neighbors.some((n) => n.id === "pl.zgorzelec" && n.kind === "road"), "Görlitz–Zgorzelec");
assert(worldBy.get("dd.frankfurt_oder").neighbors.some((n) => n.id === "pl.slubice" && n.kind === "road"), "Frankfurt (Oder)–Słubice");
assert(worldBy.get("pl.katowice").yields.includes("coal") && worldBy.get("pl.katowice").yields.includes("steel"), "Upper Silesian coal and steel");

const cs = content.world.regions.find((r) => r.id === "cs");
const csGroups = new Set(cs.subdivisions.map((s) => s.group));
assert(cs.subdivisions.length === 12 && csGroups.size === 2 && csGroups.has("Czech Socialist Republic") && csGroups.has("Slovak Socialist Republic"), "Czech and Slovak republics inside one Czechoslovakia");
assert(worldBy.get("at.vienna").neighbors.some((n) => n.id === "cs.bratislava" && n.kind === "road"), "Petržalka–Berg");

const hu = content.world.regions.find((r) => r.id === "hu");
assert(hu.subdivisions.filter((s) => s.kind === "megye").length === 19, "19 Hungarian megye");
assert(hu.subdivisions.some((s) => s.name === "Budapest"), "Budapest");
assert(hu.subdivisions.some((s) => s.name === "Győr-Sopron") && hu.subdivisions.some((s) => s.name === "Szabolcs-Szatmár"), "Hungarian megye keep their 1980s names");
assert(worldBy.get("at.eisenstadt").neighbors.some((n) => n.id === "hu.sopron" && n.kind === "road"), "Klingenbach–Sopron");

const ro = content.world.regions.find((r) => r.id === "ro");
assert(ro.subdivisions.filter((s) => s.kind === "judet").length === 40, "40 Romanian județe");
assert(ro.subdivisions.some((s) => s.kind === "municipality" && /bucharest/i.test(s.name)), "Bucharest municipality");
assert(!ro.subdivisions.some((s) => /ilfov/i.test(s.name)), "Ilfov is not restored until 1997");
assert(worldBy.get("ro.giurgiu").neighbors.some((n) => n.id === "bg.ruse" && n.kind === "road"), "Giurgiu–Ruse Friendship Bridge");
assert(worldBy.get("ro.drobeta").neighbors.some((n) => n.id === "bg.vidin" && n.kind === "sea"), "Calafat–Vidin is still a ferry");
assert(!worldBy.get("ro.drobeta").neighbors.some((n) => n.id === "bg.vidin" && n.kind === "road"), "no Calafat–Vidin bridge");

const bg = content.world.regions.find((r) => r.id === "bg");
assert(bg.subdivisions.length === 9 && bg.subdivisions.every((s) => s.kind === "oblast"), "nine Bulgarian oblasti of 1987");
assert(bg.subdivisions.some((s) => s.name === "Mikhailovgrad"), "Mikhailovgrad has not become Montana");
assert(!bg.subdivisions.some((s) => s.name === "Montana" || s.name === "Blagoevgrad"), "1987 Bulgaria is not the later 28 oblasti");
assert(worldBy.get("bg.blagoevgrad").neighbors.some((n) => n.id === "gr.serres" && n.kind === "road"), "Kulata–Promachonas");
assert(worldBy.get("bg.svilengrad").neighbors.some((n) => n.id === "tr.edirne" && n.kind === "road"), "Kapitan Andreevo");

const yu = content.world.regions.find((r) => r.id === "yu");
const yuGroups = new Set(yu.subdivisions.map((s) => s.group));
["Slovenia", "Croatia", "Bosnia and Herzegovina", "Serbia", "Montenegro", "Macedonia", "Serbia — Vojvodina", "Serbia — Kosovo"].forEach((group) => {
  assert(yuGroups.has(group), `Yugoslavia keeps ${group}`);
});
assert(worldBy.has("yu.titograd"), "Titograd has not become Podgorica");
assert(worldBy.get("yu.ljubljana").neighbors.some((n) => n.id === "at.klagenfurt" && n.kind === "road" && /Loibl/.test(n.via)), "Loibl, not the 1991 Karawanks tunnel");
assert(worldBy.get("yu.koper").neighbors.some((n) => n.id === "it.trieste" && n.kind === "road"), "Fernetti–Sežana");
assert(content.world.regions.find((r) => r.id === "al").subdivisions.length === 26, "26 Albanian districts");
assert(worldBy.get("al.kukes").yields.includes("chrome") && worldBy.get("al.gjirokaster").neighbors.some((n) => n.id === "gr.ioannina" && n.kind === "road"), "Albanian chrome and the Kakavia road");

const ru = content.world.regions.find((r) => r.id === "ru");
assert(ru.subdivisions.filter((s) => s.kind === "oblast").length === 49, "49 RSFSR oblasts");
assert(ru.subdivisions.filter((s) => s.kind === "krai").length === 6, "6 RSFSR krais");
assert(ru.subdivisions.filter((s) => s.kind === "assr").length === 16, "16 RSFSR ASSRs");
assert(ru.subdivisions.filter((s) => s.kind === "autonomous_okrug").length === 10, "10 autonomous okrugs");
assert(ru.subdivisions.filter((s) => /chechen/i.test(s.name)).length === 1, "Chechen-Ingush stays one ASSR");
assert(ru.subdivisions.some((s) => s.id === "RU-KAM"), "Kamchatka");
assert(ru.subdivisions.some((s) => s.id === "RU-SAK"), "Sakhalin");
assert(ru.subdivisions.some((s) => s.id === "RU-CHU" && s.group === "Magadan Oblast"), "Chukotka is still part of Magadan Oblast");
assert(ru.subdivisions.some((s) => s.id === "RU-KOR" && s.group === "Kamchatka Oblast"), "Koryak stays inside Kamchatka");
assert(ru.territories.some((t) => t.name === "Gorky") && ru.territories.some((t) => t.name === "Sverdlovsk") && ru.territories.some((t) => t.name === "Leningrad"), "RSFSR keeps 1980s city names");
assert(worldBy.get("ru.provideniya").lon > 170, "Chukotka is stored east of Greenwich");
assert(worldBy.get("ru.kurilsk").neighbors.every((n) => n.kind === "sea"), "the Kurils are reached by sea");
assert(worldBy.get("ru.yuzhno_sakhalinsk").neighbors.some((n) => n.id === "ru.sovetskaya_gavan" && n.kind === "sea"), "Vanino–Kholmsk ferry");
assert(!worldBy.get("ru.kaliningrad").neighbors.some((n) => n.id.startsWith("ru.") && n.kind === "road"), "Kaliningrad has no road across the republic");
assert(worldBy.get("ru.kaliningrad").neighbors.some((n) => n.id === "ru.leningrad" && n.kind === "sea"), "Kaliningrad–Leningrad is a Baltic lane");
assert(ru.territories.some((t) => t.neighbors.some((n) => n.kind === "road" && /Trans-Siberian/.test(n.via || ""))), "the Trans-Siberian is drawn as a road");
assert(worldBy.get("ru.taishet").neighbors.some((n) => n.id === "ru.bratsk" && n.kind === "road" && n.via === "BAM"), "BAM is a road");
assert(worldBy.get("ru.khabarovsk").neighbors.some((n) => n.id === "ru.vladivostok" && n.kind === "road"), "the Trans-Siberian reaches Vladivostok");
assert(worldBy.get("ru.ordzhonikidze").neighbors.some((n) => n.id === "ge.tbilisi" && n.kind === "road" && /Cross Pass/.test(n.via)), "Georgian Military Road");
assert(worldBy.get("ru.makhachkala").neighbors.filter((n) => n.id === "az.baku").length === 1 && worldBy.get("ru.makhachkala").neighbors.some((n) => n.id === "az.baku" && n.kind === "road"), "Derbent is a coastal road");
assert(worldBy.get("az.baku").neighbors.some((n) => n.id === "tm.krasnovodsk" && n.kind === "sea"), "Baku–Krasnovodsk ferry");
assert(worldBy.get("az.baku").neighbors.some((n) => n.id === "az.nakhichevan" && n.kind === "air"), "Nakhichevan is reached by air");
assert(!worldBy.get("az.baku").neighbors.some((n) => n.id === "az.nakhichevan" && n.kind === "road"), "no invented road across Armenia to Nakhichevan");
assert(worldBy.get("tj.dushanbe").neighbors.some((n) => n.id === "tj.khujand" && n.kind === "road" && /Anzob/.test(n.via)), "Anzob Pass");
assert(worldBy.get("kg.osh").neighbors.some((n) => n.id === "tj.khorog" && n.kind === "road" && /Pamir/.test(n.via)), "Pamir Highway");
assert(worldBy.get("kg.frunze").neighbors.some((n) => n.id === "kz.alma_ata" && n.kind === "road" && /Kordai/.test(n.via)), "Kordai pass");
assert(worldBy.get("ru.mirny").yields.includes("diamonds") && worldBy.get("kz.aktyubinsk").yields.includes("chrome"), "Mirny diamonds and Aktyubinsk chrome");

const ua = content.world.regions.find((r) => r.id === "ua");
assert(ua.subdivisions.some((s) => s.id === "UA-CR" && s.name === "Crimea"), "Crimea is a Ukrainian oblast");
assert(!ru.subdivisions.some((s) => /crimea/i.test(s.name)), "Crimea is not drawn as a Russian subdivision");
assert(worldBy.get("ua.simferopol").neighbors.some((n) => n.id === "ua.kherson" && n.kind === "road"), "Perekop isthmus");
assert(!worldBy.get("ua.simferopol").neighbors.some((n) => n.id.startsWith("ru.") && n.kind === "road"), "no Kerch bridge");
assert(worldBy.get("ua.sevastopol").neighbors.some((n) => n.id === "ru.novorossiysk" && n.kind === "sea"), "Crimea to Novorossiysk is a sea lane");
assert(worldBy.get("ua.odessa").neighbors.some((n) => n.id === "bg.varna" && n.kind === "sea"), "Black Sea lane");
assert(worldBy.get("fi.helsinki").neighbors.some((n) => n.id === "ee.tallinn" && n.kind === "sea"), "Tallinn–Helsinki ferry");
assert(worldBy.get("fi.helsinki").neighbors.some((n) => n.id === "ru.leningrad" && n.kind === "sea"), "Leningrad Baltic shipping");
assert(worldBy.get("se.stockholm").neighbors.some((n) => n.id === "lv.riga" && n.kind === "sea"), "Stockholm–Riga");
assert(worldBy.get("pl.gdansk").neighbors.some((n) => n.id === "ru.kaliningrad" && n.kind === "sea"), "Gdańsk–Kaliningrad");
assert(worldBy.get("us.nome").neighbors.some((n) => n.id === "ru.provideniya" && n.kind === "sea"), "Bering Strait is a sea lane");
assert(!worldBy.get("us.nome").neighbors.some((n) => n.id === "ru.provideniya" && n.kind === "road"), "no Bering highway");
assert(worldBy.get("no.vadso").neighbors.some((n) => n.id === "ru.murmansk" && n.kind === "road"), "Kirkenes–Borisoglebsk");
assert(worldBy.get("tr.artvin").neighbors.some((n) => n.id === "ge.batumi" && n.kind === "road"), "Sarpi");
assert(worldBy.get("tr.kars").neighbors.some((n) => n.id === "am.leninakan" && n.kind === "trail"), "Akyaka stays closed");
assert(worldBy.get("ru.petropavlovsk").neighbors.some((n) => n.id === "ru.magadan" && n.kind === "sea"), "Sea of Okhotsk");
assert(atlasMode({ atlas: "su", z: 2, focus: null }) === "world", "the USSR close-up labels capitals, not every city");
assert(atlasMode({ atlas: "eu", z: 2, focus: null }) === "world", "the Europe close-up stays in world mode");
assert(worldBy.get("tm.kushka").neighbors.some((n) => n.id === "tm.mary" && n.kind === "road"), "Kushka is the Turkmen border spur");

const meIds = ["ma", "dz", "tn", "ly", "eg", "sdn", "dj", "il", "lb", "sy", "jo", "iq", "kw", "bh", "qa", "ae", "om", "sa", "ye", "yd", "ir", "af"];
meIds.forEach((id) => {
  const region = content.world.regions.find((r) => r.id === id);
  const count = counts.find((c) => c.id === id);
  assert(region && region.playable === false && region.status === "occupied" && region.reach === "later", `${id} stays dormant`);
  assert(count.officers === rosterSize(count.territories) && count.playable === 0, `${id} roster stays normalized`);
  assert(region.subdivisions.every((s) => !earlySubs.has(s.id) && s.id.startsWith(`${region.country}-`)), `${id} subdivision ids stay prefixed`);
  const records = toOfficerRecords(region);
  assert(records.length === count.officers && records.every((o) => o.dormant && o.world && o.fictional && o.faction == null), `${id} officers stay off the week-0 list`);
  assert(region.officers.every((o) => /fiction/i.test(o.bio)), `${id} bios stay fictional`);
});
assert(!content.world.regions.some((r) => r.id === "eh" || r.id === "ym" || r.id === "ws"), "Western Sahara and united Yemen are not countries");
assert(content.world.regions.find((r) => r.id === "ye") && content.world.regions.find((r) => r.id === "yd"), "North and South Yemen are separate");
assert(content.world.regions.find((r) => r.id === "ye").subdivisions.length === 11, "eleven North Yemen governorates");
assert(content.world.regions.find((r) => r.id === "yd").subdivisions.length === 6, "six South Yemen governorates");
const ma = content.world.regions.find((r) => r.id === "ma");
assert(ma.subdivisions.filter((s) => s.group === "Western Sahara (Moroccan administration)").length === 4, "Western Sahara is four Moroccan provinces");
assert(content.world.regions.find((r) => r.id === "dz").subdivisions.length === 48, "48 Algerian wilayas");
assert(content.world.regions.find((r) => r.id === "ly").subdivisions.length === 46, "46 Libyan baladiyat");
assert(content.world.regions.find((r) => r.id === "tn").subdivisions.length === 23, "23 Tunisian governorates");
const eg = content.world.regions.find((r) => r.id === "eg");
assert(eg.subdivisions.length === 26, "26 Egyptian governorates");
assert(eg.subdivisions.filter((s) => s.group === "Sinai (returned 1982)").length === 2, "Sinai is Egyptian");
assert(!content.world.regions.find((r) => r.id === "il").subdivisions.some((s) => /sinai/i.test(s.name)), "Sinai is not an Israeli district");
const il = content.world.regions.find((r) => r.id === "il");
assert(il.subdivisions.filter((s) => s.kind === "zone" && s.group === "Israeli occupation").length === 3, "West Bank, Gaza, and the Golan are zones");
assert(!content.world.regions.some((r) => r.id === "gz" || r.name === "West Bank"), "the occupied territories are not countries");
const lb = content.world.regions.find((r) => r.id === "lb");
assert(lb.subdivisions.filter((s) => s.kind === "zone" && s.name === "Israeli security zone").length === 1, "the security zone is inside Lebanon");
assert(!content.world.regions.some((r) => /security zone/i.test(r.name)), "the security zone is not a country");
assert(content.world.regions.find((r) => r.id === "sdn").subdivisions.length === 9, "undivided Sudan has nine regions");
assert(!content.world.regions.some((r) => r.id === "ss"), "South Sudan is not a state");
assert(content.world.regions.find((r) => r.id === "ir").subdivisions.filter((s) => s.group === "Iran-Iraq front").length === 4, "four Iranian front provinces");
assert(content.world.regions.find((r) => r.id === "iq").subdivisions.filter((s) => s.group === "Iran-Iraq front").length === 5, "five Iraqi front provinces");
assert(content.world.regions.find((r) => r.id === "ir").playable === false && content.world.regions.find((r) => r.id === "iq").playable === false, "the war stays dormant");
assert(content.world.regions.find((r) => r.id === "af").subdivisions.length === 29, "29 Afghan provinces");
assert(content.world.regions.find((r) => r.id === "sa").subdivisions.length === 14, "fourteen Saudi emirates");
assert(worldBy.get("af.charikar").neighbors.some((n) => n.id === "af.puli_khumri" && n.kind === "road" && n.via === "Salang tunnel"), "Salang tunnel");
assert(worldBy.get("af.hairatan").neighbors.some((n) => n.id === "uz.termez" && n.kind === "road" && /Friendship Bridge/.test(n.via)), "Termez bridge");
assert(worldBy.get("af.sher_khan").neighbors.some((n) => n.id === "tj.kurgan_tyube" && n.kind === "road"), "Sher Khan Bandar");
assert(worldBy.get("af.torghundi").neighbors.some((n) => n.id === "tm.kushka" && n.kind === "road"), "Kushka-Torghundi");
assert(worldBy.get("eg.port_said").neighbors.some((n) => n.id === "eg.suez" && n.kind === "sea" && n.via === "Suez Canal"), "Suez Canal");
assert(!worldBy.get("eg.port_said").neighbors.some((n) => n.id === "eg.suez" && n.kind === "road"), "the canal is not also a road");
assert(worldBy.get("yd.aden").neighbors.some((n) => n.id === "dj.djibouti" && n.kind === "sea" && /Bab-el-Mandeb/.test(n.via)), "Bab-el-Mandeb");
assert(worldBy.get("om.khasab").neighbors.some((n) => n.id === "ir.bandar_abbas" && n.kind === "sea" && /Hormuz/.test(n.via)), "Strait of Hormuz");
assert(worldBy.get("gi.gibraltar").neighbors.some((n) => n.id === "ma.tangier" && n.kind === "sea"), "Gibraltar tie-in");
assert(!worldBy.get("ma.tangier").neighbors.some((n) => n.id === "es.algeciras" && n.kind === "road"), "no road across the Strait");
assert(worldBy.get("es.ceuta").neighbors.some((n) => n.id === "ma.tetouan" && n.kind === "road"), "Ceuta land frontier");
assert(worldBy.get("es.melilla").neighbors.some((n) => n.id === "ma.nador" && n.kind === "road"), "Melilla land frontier");
assert(worldBy.get("tr.antakya").neighbors.some((n) => n.id === "sy.aleppo" && n.kind === "road"), "Bab al-Hawa");
assert(worldBy.get("tr.siirt").neighbors.some((n) => n.id === "iq.zakho" && n.kind === "road"), "Habur");
assert(worldBy.get("tr.agri").neighbors.some((n) => n.id === "ir.maku" && n.kind === "road"), "Gurbulak-Bazargan");
assert(worldBy.get("ir.astara").neighbors.some((n) => n.id === "az.baku" && n.kind === "road"), "Astara");
assert(worldBy.get("ir.jolfa").neighbors.some((n) => n.id === "az.nakhichevan" && n.kind === "road"), "Jolfa");
assert(!worldBy.get("az.baku").neighbors.some((n) => n.id === "az.nakhichevan" && n.kind === "road"), "Jolfa does not invent a road across Armenia");
assert(worldBy.get("iq.basra").neighbors.some((n) => n.id === "ir.ahvaz" && n.kind === "trail"), "the front is not an open road");
assert(!worldBy.get("iq.basra").neighbors.some((n) => n.id === "ir.ahvaz" && n.kind === "road"), "no highway across the front");
assert(worldBy.get("sa.dammam").yields.includes("oil") && worldBy.get("ir.ahvaz").yields.includes("oil") && worldBy.get("ly.surt").yields.includes("oil"), "oil is the major yield");
assert(worldBy.get("sa.dammam").neighbors.some((n) => n.id === "bh.manama" && n.kind === "road" && /Causeway/.test(n.via)), "King Fahd Causeway");
assert(atlasMode({ atlas: "me", z: 2, focus: null }) === "world", "the Middle East close-up stays in world mode");

const ssaIds = ["mr", "sn", "gm", "gw", "gn", "sl", "lr", "ci", "gh", "tg", "bj", "bf", "ml", "ne", "ng", "cm", "td", "cf", "gq", "ga", "cg", "zr", "st", "et", "so", "ke", "ug", "rw", "bi", "tz", "ao", "zm", "mw", "mz", "zw", "bw", "za", "ls", "sz", "mg", "km", "mu", "sc", "cv", "re", "yt"];
ssaIds.forEach((id) => {
  const region = content.world.regions.find((r) => r.id === id);
  const count = counts.find((c) => c.id === id);
  assert(region && region.playable === false && region.status === "occupied" && region.reach === "later", `${id} stays dormant`);
  assert(count.officers === rosterSize(count.territories) && count.playable === 0, `${id} roster stays normalized`);
  assert(region.subdivisions.every((s) => !earlySubs.has(s.id) && s.id.startsWith(`${region.country}-`)), `${id} subdivision ids stay prefixed`);
  const records = toOfficerRecords(region);
  assert(records.length === count.officers && records.every((o) => o.dormant && o.world && o.fictional && o.faction == null), `${id} officers stay off the week-0 list`);
  assert(region.officers.every((o) => /fiction/i.test(o.bio)), `${id} bios stay fictional`);
});
assert(!content.world.regions.some((r) => r.id === "er" || r.name === "Eritrea" || r.name === "Rhodesia" || r.name === "Upper Volta" || r.id === "na" || r.name === "Namibia" || r.id === "cd"), "Eritrea, Rhodesia, Upper Volta, Namibia, and DR Congo are not countries");
const et = content.world.regions.find((r) => r.id === "et");
assert(et.subdivisions.length === 14 && et.subdivisions.some((s) => s.name === "Eritrea" && s.group === "Eritrea (Derg province)"), "Eritrea is one Derg province");
const so = content.world.regions.find((r) => r.id === "so");
assert(so.subdivisions.length === 18 && so.subdivisions.some((s) => s.name === "Awdal") && so.subdivisions.some((s) => s.name === "Sool"), "Somalia stays undivided, with Awdal and Sool");
assert(!content.world.regions.some((r) => r.name === "Somaliland"), "Somaliland is not a country");
const zr = content.world.regions.find((r) => r.id === "zr");
assert(zr.name === "Zaire" && zr.subdivisions.length === 9 && zr.subdivisions.filter((s) => /kivu/i.test(s.name)).length === 1, "Zaire keeps one Kivu");
assert(zr.subdivisions.some((s) => s.name === "Shaba"), "Shaba has not become Katanga");
const za = content.world.regions.find((r) => r.id === "za");
assert(za.subdivisions.filter((s) => s.kind === "province").length === 4, "South Africa has four provinces");
assert(za.subdivisions.filter((s) => s.kind === "zone").length === 10, "each homeland is a zone");
assert(za.subdivisions.filter((s) => s.group === "South West Africa (South African administration)").length === 12, "South West Africa is twelve districts inside South Africa");
assert(worldBy.get("za.walvis_bay").subdivision === "ZA-CP", "Walvis Bay stays in the Cape Province");
assert(content.world.regions.find((r) => r.id === "bf").name === "Burkina Faso" && content.world.regions.find((r) => r.id === "bf").subdivisions.length === 30, "thirty Burkina provinces");
assert(content.world.regions.find((r) => r.id === "zw").name === "Zimbabwe", "Rhodesia is already Zimbabwe");
const ao = content.world.regions.find((r) => r.id === "ao");
const mz = content.world.regions.find((r) => r.id === "mz");
assert(ao.subdivisions.length === 18 && ao.subdivisions.filter((s) => s.group === "Angolan civil war").length === 6 && ao.playable === false, "Angola marks six front provinces and stays dormant");
assert(mz.subdivisions.filter((s) => s.group === "Mozambican civil war").length === 6 && mz.playable === false, "Mozambique marks six front provinces and stays dormant");
assert(content.world.regions.find((r) => r.id === "ng").subdivisions.length === 22, "Nigeria is 21 states plus the FCT");
assert(!content.world.regions.find((r) => r.id === "ng").subdivisions.some((s) => s.name === "Delta" || s.name === "Zamfara" || s.name === "Ebonyi"), "the 1991 and 1996 Nigerian states are not drawn");
assert(content.world.regions.find((r) => r.id === "km").subdivisions.length === 3 && !content.world.regions.find((r) => r.id === "km").subdivisions.some((s) => /mayotte/i.test(s.name)), "Comoros is three islands");
assert(/French/.test(content.world.regions.find((r) => r.id === "re").notes) && /French/.test(content.world.regions.find((r) => r.id === "yt").notes), "Reunion and Mayotte are French");
assert(content.world.regions.find((r) => r.id === "cv").subdivisions.length === 9, "nine Cape Verde islands");
assert(content.world.regions.find((r) => r.id === "mg").subdivisions.length === 6, "six Madagascar provinces");
assert(worldBy.get("tz.mbeya").neighbors.some((n) => n.id === "zm.kapiri" && n.kind === "rail" && n.via === "TAZARA"), "TAZARA");
assert(!worldBy.get("tz.mbeya").neighbors.some((n) => n.id === "zm.kapiri" && n.kind === "road"), "TAZARA is not also a road");
assert(worldBy.get("ao.lobito").neighbors.some((n) => n.id === "ao.benguela" && n.kind === "rail" && /Benguela/.test(n.via)), "Benguela railway");
assert(worldBy.get("ao.dilolo").neighbors.some((n) => n.id === "zr.kolwezi" && n.kind === "rail" && /Benguela/.test(n.via)), "Benguela reaches Kolwezi");
assert(worldBy.get("ao.cabinda").neighbors.some((n) => n.id === "ao.luanda" && n.kind === "sea"), "Cabinda is reached by sea");
assert(!worldBy.get("ao.cabinda").neighbors.some((n) => n.id.startsWith("zr.")), "no Cabinda road through Zaire");
assert(worldBy.get("za.walvis_bay").neighbors.some((n) => n.id === "za.cape_town" && n.kind === "sea" && /Cape/.test(n.via)), "Cape route");
assert(worldBy.get("za.cape_town").neighbors.some((n) => n.id === "za.durban" && n.kind === "sea"), "Cape route east");
assert(worldBy.get("za.durban").neighbors.some((n) => n.id === "mz.maputo" && n.kind === "sea"), "Cape route to Maputo");
assert(worldBy.get("mz.beira").neighbors.some((n) => n.id === "mg.toamasina" && n.kind === "sea" && /Mozambique Channel/.test(n.via)), "Mozambique Channel");
assert(worldBy.get("sn.dakar").neighbors.some((n) => n.id === "gm.banjul" && n.kind === "sea" && /Gulf of Guinea/.test(n.via)), "Gulf of Guinea");
assert(worldBy.get("ng.lagos").neighbors.some((n) => n.id === "cm.douala" && n.kind === "sea"), "Gulf of Guinea east");
assert(worldBy.get("et.massawa").neighbors.some((n) => n.id === "sdn.port_sudan" && n.kind === "sea" && /Red Sea/.test(n.via)), "Red Sea tie-in");
assert(worldBy.get("so.berbera").neighbors.some((n) => n.id === "yd.aden" && n.kind === "sea"), "Gulf of Aden");
assert(worldBy.get("et.assab").neighbors.some((n) => n.id === "dj.djibouti" && n.kind === "sea"), "Assab-Djibouti");
assert(worldBy.get("ma.dakhla").neighbors.some((n) => n.id === "mr.nouadhibou" && n.kind === "road"), "Dakhla-Nouadhibou");
assert(worldBy.get("dz.tamanrasset").neighbors.some((n) => n.id === "ne.arlit" && n.kind === "road" && /Trans-Sahara/.test(n.via)), "Trans-Sahara");
assert(worldBy.get("ly.murzuq").neighbors.some((n) => n.id === "td.faya" && n.kind === "trail" && /Aouzou/.test(n.via)), "Aouzou is a trail");
assert(!worldBy.get("ly.murzuq").neighbors.some((n) => n.id === "td.faya" && n.kind === "road"), "Aouzou is not a highway");
assert(worldBy.get("sdn.el_fasher").neighbors.some((n) => n.id === "td.abeche" && n.kind === "road"), "Adre");
assert(worldBy.get("sdn.juba").neighbors.some((n) => n.id === "ug.gulu" && n.kind === "road" && /Nimule/.test(n.via)), "Nimule");
assert(worldBy.get("dj.djibouti").neighbors.some((n) => n.id === "et.dire_dawa" && n.kind === "rail" && /Franco-Ethiopian/.test(n.via)), "Franco-Ethiopian railway");
assert(!worldBy.get("dj.djibouti").neighbors.some((n) => n.id === "et.dire_dawa" && n.kind === "road"), "the railway is not also a road");
assert(worldBy.get("zr.kolwezi").yields.includes("cobalt"), "Shaba cobalt");
assert(catalog.resources.some((r) => r.id === "cobalt"), "cobalt is on the resource list");
assert(atlasMode({ atlas: "ssa", z: 2, focus: null }) === "world", "the Africa close-up stays in world mode");

console.log("ok world catalog");
counts.forEach((c) => {
  console.log(`  ${c.name}: ${c.states} states, ${c.districts} other units, ${c.territories} territories, ${c.officers} officers`);
});
